%==============================================================================
%  Methods in Molecular Biology chapter — DRAFT
%
%  Manisha Chaudhary, Song Li (Virginia Tech)
%
%  NOTES BEFORE SUBMISSION:
%   - Text in \PLACEHOLDER{} requires a decision or a value you must supply.
%   - EVERY reference must be verified against the actual paper. They were
%     drafted without database access.
%   - Springer prefers Word for MiMB. Confirm with your volume editor that
%     LaTeX is acceptable; if not, compile and export, or paste section text
%     into their Word template.
%   - This uses plain article class so it compiles on Overleaf with no setup.
%     If the editor supplies svmult.cls, migrate the preamble.
%==============================================================================

\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage[margin=1in]{geometry}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{amssymb}
\usepackage{enumitem}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\doublespacing

% Placeholder marker — makes unfinished items impossible to miss
\newcommand{\PLACEHOLDER}[1]{\textbf{\color{red}[#1]}}

% Cross-reference helpers matching MiMB house style
\newcommand{\seenote}[1]{(\textit{see} \textbf{Note~#1})}
\newcommand{\seenotes}[1]{(\textit{see} \textbf{Notes~#1})}
\newcommand{\seesub}[1]{(\textit{see} \textbf{Subheading~#1})}

%------------------------------------------------------------------------------
% FIGURE HANDLING
%
% \FIGBOX{filename-without-extension}{width as fraction of textwidth}
%
% If the image file exists in the project, it is included.
% If not, a labelled grey placeholder box is drawn instead, so the document
% always compiles and you can see exactly which figures are still missing.
%
% Upload your PNGs to the Overleaf project's figures/ folder to replace the
% placeholders. Expected paths:
%     figures/fig1_workflow.png                     (does not exist yet - must be made)
%     figures/fig1_umap_celltypes.png
%     figures/fig2_tf_featureplots.png
%     figures/fig3_marker_dotplot.png
%     figures/fig4_networks_mature-endodermis.png
%     figures/fig4_networks_dividing_cells.png
%     figures/fig4_networks_exodermis.png
%------------------------------------------------------------------------------
\usepackage{tikz}
\newcommand{\FIGBOX}[2]{%
  \IfFileExists{#1.png}{\includegraphics[width=#2\textwidth]{#1.png}}{%
  \IfFileExists{#1.pdf}{\includegraphics[width=#2\textwidth]{#1.pdf}}{%
    \fbox{\begin{minipage}[c][0.34\textwidth][c]{#2\textwidth}%
      \centering\color{gray}%
      {\ttfamily\small\detokenize{#1}}\\[6pt]
      {\rmfamily\small image file not found in project}%
    \end{minipage}}}}%
}

\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  framesep=4pt,
  columns=fullflexible,
  showstringspaces=false,
  aboveskip=8pt,
  belowskip=8pt
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

\begin{document}

\begin{singlespace}
\begin{flushleft}
{\Large\bfseries Single-Cell Gene Regulatory Network Analysis Using an AI Coding Agent\par}

\vspace{1em}

{\large Manisha Chaudhary\textsuperscript{1}, Song Li\textsuperscript{1}\par}

\vspace{0.5em}

\textsuperscript{1}School of Plant and Environmental Sciences, Virginia Polytechnic Institute and State University, Blacksburg, VA, USA

\vspace{0.5em}

\textbf{Corresponding author:} Song Li \\
\textbf{Email:} songli@vt.edu

\vspace{0.5em}

\textit{Running head:} Agent-directed single-cell GRN analysis
\end{flushleft}
\end{singlespace}

\vspace{1em}
\hrule
\vspace{1em}

%==============================================================================
\section*{Abstract}
%==============================================================================

Large language model coding agents can execute multi-step bioinformatics analyses from a natural-language specification, writing and running the underlying code themselves. This changes what must be archived for an analysis to be reproducible: the specification, rather than the code, becomes the primary artifact. In this chapter we describe a workflow in which a coding agent executes a single-cell gene regulatory network analysis end to end, directed by a single versioned prompt file. The major steps are: (1) assemble the expression data and a transcription factor list in the same gene identifier namespace; (2) write a prompt file specifying inputs, fixed parameters, expected intermediate values, and required outputs; (3) direct the agent to export the expression matrix, filter nuclei, and infer co-expression networks with GRNBoost2 on a high-performance computing cluster; and (4) select regulators and generate figures. We demonstrate the workflow on published single-nucleus RNA-seq data from sorghum root, inferring networks for three cell types from 14,792 nuclei and 1,022 expressed transcription factors. We report the agent's behavior in detail, including the environment and dependency failures it diagnosed. The protocol applies to other species and datasets, and the Notes address failure modes specific to agent-directed analysis.

\vspace{0.8em}
\textbf{Key words} Gene regulatory network, Single-cell RNA sequencing, Large language model, Coding agent, Reproducibility, Transcription factor, Co-expression network, Sorghum, Workflow automation

\vspace{1em}
\hrule

%==============================================================================
\section{Introduction}
%==============================================================================

Single-cell and single-nucleus RNA sequencing are now routinely applied in plant biology to study development and stress response across an increasing number of species \cite{ryu2019,denyer2019,dorrity2021,shahan2022,marand2021}. A common downstream objective is to infer gene regulatory networks (GRNs): sets of relationships linking transcription factors (TFs) to the genes they may regulate. Several computational strategies exist, including approaches based on motif enrichment in promoter regions \cite{kulkarni2018,grant2011}, machine learning on sequence features \cite{song2020}, and regression-based inference from expression data alone \cite{moerman2019,vandesande2020}. A previous chapter in this series described a pipeline integrating scRNA-seq and scATAC-seq data with motif analysis and machine learning for GRN construction in Arabidopsis \cite{chau2023}.

These pipelines share a practical characteristic. They consist of many small steps, each requiring correct file formats, matched gene identifier namespaces, and compatible software versions. In our experience the majority of effort in executing such a pipeline is spent not on the analysis itself but on reconciling these details. This effort is rarely documented and is consequently repeated by each new user.

Large language model coding agents offer a different way of organizing this work. An agent is given a task description in natural language and then writes, executes, and debugs code to accomplish it, iterating when a step fails. The analyst specifies \textit{what} must be produced; the agent determines \textit{how}. \PLACEHOLDER{Add a citation for agentic coding tools here, or state that no peer-reviewed reference is yet available.}

This shifts what should be archived. In a conventional pipeline the scripts are the reproducible artifact. In an agent-directed pipeline the agent may write different code on different runs, so the scripts are an output rather than an input. What persists across runs is the specification. We therefore treat a single versioned prompt file as the primary artifact of the analysis, accompanied by a pinned software environment and a run log recording every deviation \seenote{16}.

We state at the outset what this protocol does and does not establish. The networks produced are co-expression associations inferred by GRNBoost2 \cite{moerman2019} from expression data alone. They are not validated regulatory relationships: no transcription factor binding site information is used to prune edges, and no experimental validation is performed. Edges should be read as hypotheses for further testing \seenotes{11 and 12}.

As a worked example we use published single-nucleus RNA-seq data from sorghum (\textit{Sorghum bicolor}) root \cite{baumgart2025}. This dataset was selected because it is public, annotated with 27 cell type labels, and processed to a stage requiring no reclustering. The biology of sorghum root is not the subject of this chapter; the dataset demonstrates the workflow, and the protocol applies without modification to other species and tissues for which an expression matrix and a transcription factor list are available. The overall workflow is shown in Fig.~\ref{fig:workflow}.

%==============================================================================
\section{Materials}
%==============================================================================

The prompt file, conda environment specification, run log, and all scripts generated during the run are available at \url{https://github.com/mchaudhary22/Book_Chapter_GRN_082026}. Readers reproducing this protocol require the prompt file and the environment specification; the scripts are provided for reference but are regenerated by the agent at run time \seenote{15}.

\subsection{Computing environment}

Virginia Tech Advanced Research Computing TinkerCliffs cluster, running Linux under the SLURM workload manager. All computation was performed on compute nodes; login nodes were used only for file management. The network inference step was submitted as a batch job \seenote{6}.

Development was initially attempted on a Windows workstation and migrated to the cluster \seenote{7}.

\subsection{Coding agent}

Claude Code \PLACEHOLDER{version --- record from your session}, accessed through the cluster terminal. The agent was directed by a single prompt file, \texttt{prompt.md}, held at the project root and placed under version control.

\subsection{R environment}

Modules \texttt{R/4.4.2-gfbf-2024a} and \texttt{R-bundle-Bioconductor/3.20-foss-2024a-R-4.4.2}, providing R 4.4.2, Seurat 5.3.1, SeuratObject 5.3.0, and hdf5r 1.3.11. SeuratDisk was not available and was not installed; the Seurat-to-AnnData conversion used a two-step fallback \seesub{3.5} and \seenote{8}.

\subsection{Python environment}

A dedicated conda environment, created before the agent was invoked and exported to \texttt{environment.yml}:

\begin{lstlisting}[language=bash]
module load Miniconda3/25.11.1-1
mamba create -p ~/.conda/envs/grn python=3.10
source activate ~/.conda/envs/grn
pip install pyscenic scanpy anndata pandas numpy \
    matplotlib seaborn openpyxl "setuptools<81"
conda env export --no-builds > environment.yml
\end{lstlisting}

Resolved versions: Python 3.10.0, pySCENIC 0.12.1, arboreto 0.1.6, ctxcore 0.2.0, scanpy 1.10.4, anndata 0.11.4, pandas 2.3.3, numpy 1.23.5, matplotlib 3.10.9, seaborn 0.13.2, openpyxl 3.1.5, setuptools 80.10.2, dask 2024.8.2, distributed 2024.8.2.

Three constraints on this environment are required and are described in \textbf{Notes~1--3}. The environment was verified before use by running GRNBoost2 on a synthetic matrix of 50 cells and 20 genes with 5 transcription factors, which returned the expected 95 edges \seenote{4}.

\subsection{Input data}

\begin{enumerate}[leftmargin=*]
\item \textbf{Expression atlas.} \texttt{GSE297576\_seurat\_object.bicolor\_root\_atlas.RDS} (1,314,285,536 bytes), a processed Seurat v5 object containing 19,316 nuclei, downloaded from Gene Expression Omnibus accession GSE297576 \cite{baumgart2025}. The object contains an \texttt{RNA} assay (25,464 features, raw counts) and an \texttt{SCT} assay (23,974 features), with \texttt{SCT} set as the default assay \seenote{9}. Precomputed PCA and UMAP reductions are present and were used directly. Nuclei from multiplexed sequencing runs were separated by species by the original authors. Cell type labels are in the \texttt{celltype} metadata column (27 levels); the \texttt{cluster\_label.long} column was not used \seenote{10}. Gene identifiers follow the \textit{Sorghum bicolor} RTx430 v2.1 annotation (\texttt{SbiRTX430.*}).

\item \textbf{Transcription factor list.} \texttt{Sbi\_RTx430\_TF\_list.txt}, 1,827 transcription factors in the RTx430 namespace with family assignments, generated by submitting the RTx430 primary-transcript proteome (34,601 sequences) to the PlantTFDB prediction server \cite{tian2020,jin2017}. The rationale for generating this list rather than downloading the precomputed \textit{Sorghum bicolor} list is given in \textbf{Subheading~3.2} and \textbf{Notes~13 and 14}.

\item \textbf{Marker genes.} \texttt{41477\_2025\_2047\_MOESM3\_ESM.xlsx}, sheet \texttt{TableS10.sorghum\_lit\_markers}: 50 rows of literature-curated sorghum root markers with identifiers in both the RTx430 and BTx623 namespaces, from the supplementary tables of ref.~\cite{baumgart2025}.
\end{enumerate}

Large files were downloaded using resumable transfer \seenote{5}:

\begin{lstlisting}[language=bash]
curl -L -C "-" --retry 30 --retry-delay 5 --retry-all-errors \
  -o "GSE297576_seurat_object.bicolor_root_atlas.RDS" \
  "https://www.ncbi.nlm.nih.gov/geo/download/?acc=GSE297576&format=file&file=GSE297576%5Fseurat%5Fobject%2Ebicolor%5Froot%5Fatlas%2ERDS"
\end{lstlisting}

\subsection{Project structure}

The agent was directed to work within a fixed directory layout separating read-only inputs from generated outputs, so that individual stages can be re-run independently:

\begin{lstlisting}
project/
|-- prompt.md            # the specification artifact
|-- environment.yml      # pinned software environment
|-- data/raw/            # read-only inputs
|-- data/processed/      # intermediate files
|-- scripts/             # written by the agent
|-- results/             # edge tables, TF selections
|-- figures/
`-- logs/run_log.md
\end{lstlisting}

%==============================================================================
\section{Methods}
%==============================================================================

\subsection{Overview}

The workflow comprises four stages: preparing inputs (\textbf{Subheadings~3.2--3.4}), exporting and filtering the expression matrix (\textbf{Subheading~3.5}), inferring networks (\textbf{Subheading~3.7}), and selecting regulators and generating figures (\textbf{Subheadings~3.8--3.9}). Stages 3.5 onward are executed by the agent from the prompt file; stages 3.2--3.4 are performed by the analyst beforehand.

\subsection{Constructing a transcription factor list in the correct namespace}

GRNBoost2 requires a list of candidate regulators. For most species this can be downloaded directly from PlantTFDB \cite{tian2020,jin2017}. Here a direct download was not usable, and the reason generalizes to any dataset aligned to a non-reference assembly \seenote{13}.

\begin{enumerate}[leftmargin=*]
\item Determine the genome assembly used to produce the expression matrix. Here the gene identifiers were of the form \texttt{SbiRTX430.01G002500}, corresponding to the RTx430 v2.1 assembly.

\item Compare this with the namespace of the available transcription factor list. The PlantTFDB \textit{Sorghum bicolor} list uses \texttt{Sobic.*} identifiers from the BTx623 v3.1 assembly. Intersecting the two gives zero matches.

\item Do not assume that structurally similar identifiers can be converted by string transformation. Testing a naive transform against 48 known identifier pairs --- available because the marker table in ref.~\cite{baumgart2025} reports both namespaces --- produced one match, consistent with coincidence \seenote{13}.

\item Determine whether a cross-reference between the assemblies already exists. The orthology tables distributed with ref.~\cite{baumgart2025} contain only RTx430 identifiers and are therefore unusable as an intra-species crosswalk.

\item Generate the transcription factor list directly in the required namespace. Submit the primary-transcript proteome for the assembly used by the expression data to the PlantTFDB prediction server (\url{https://planttfdb.gao-lab.org/prediction.php}), which applies the same family assignment rules used to build the database. Save the returned list immediately; results are held in a temporary session directory.

\item Strip transcript suffixes to obtain gene-level identifiers. For a primary-transcript proteome all entries carry the \texttt{.1.p} suffix and stripping yields a one-to-one mapping with no duplicates.
\end{enumerate}

This procedure returned 1,827 transcription factors across 56 families. As a check, the family composition was compared with the precomputed BTx623 list \seenote{14}.

\subsection{Writing the prompt file}

The prompt file is the reproducible artifact of the analysis and should be written before the agent is invoked. We found the following elements necessary.

\begin{enumerate}[leftmargin=*]
\item \textbf{Role and constraints.} State that the agent must write numbered scripts, checkpoint every stage to disk, and never modify files in the read-only input directory.

\item \textbf{Cluster policy.} State explicitly that computationally expensive steps must be submitted through the scheduler and never run on a login node \seenote{6}.

\item \textbf{Environment.} Name the conda environment explicitly and instruct the agent not to install packages without reporting first. An agent that silently installs dependencies produces an environment that cannot be reproduced from the exported specification.

\item \textbf{Inputs.} List each input file with its expected dimensions and identifier format.

\item \textbf{Fixed parameters.} Specify every analytical choice: the metadata column holding cell type labels, the nucleus filtering criterion, the assay and data layer, the detection threshold for regulators, the target cell types, and the random seed.

\item \textbf{Expected intermediate values.} State the number of nuclei expected after filtering and the number of regulators expected to survive detection filtering. This is the single most useful element of the prompt: it converts silent errors into loud ones \seenotes{9 and 17}.

\item \textbf{Known dependency issues.} Where a required fix is already known, state it. The three constraints in \textbf{Notes~1--3} were included in the prompt and applied without rediscovery.

\item \textbf{Stop conditions.} Instruct the agent to stop and report rather than proceed when an observed count differs from the stated expectation, and to fail loudly rather than substitute defaults.
\end{enumerate}

The prompt is invoked by directing the agent to read it, rather than by pasting its contents into the terminal:

\begin{lstlisting}
Read prompt.md and execute it. Start with Step 00 and stop after
Step 01 so I can check the intermediate outputs before the
inference run.
\end{lstlisting}

Stopping the agent at a checkpoint before the computationally expensive step allows intermediate values to be verified cheaply.

\subsection{Environment reconnaissance and construction}

Before writing pipeline code, direct the agent to report the available modules, whether Seurat 5 is provided by a module or must be installed to a personal library, the available scheduler partitions and their limits, disk quotas, and whether compute nodes have outbound network access. These findings determine the scheduler resource request and the export route, and cannot be assumed. Then build and export the environment as in \textbf{Subheading~2.4}, and verify it with the synthetic test \seenote{4}.

\subsection{Exporting and filtering the expression matrix}

\begin{enumerate}[leftmargin=*]
\item Load the Seurat object and identify the metadata column recording doublet status. Rather than assuming a column name, scan the metadata for a column containing the value \texttt{Singlet}; here exactly one column matched.

\item Subset to singlet nuclei. Doublets create apparent co-expression between genes belonging to different cell types, which is precisely the artifact that regression-based inference interprets as a regulatory relationship. Filtering reduced 19,316 nuclei to 14,792 (76.6\% retained). Per-cell-type counts are given in Table~\ref{tab:celltypes}; retention is not uniform across cell types \seenote{18}.

\item Extract raw counts from the \texttt{RNA} assay explicitly. Do not use SCT residuals \seenote{9}.

\item Convert to AnnData format. Where SeuratDisk is available, \texttt{SaveH5Seurat} followed by \texttt{Convert} is the direct route. Where it is not, write the counts matrix in MatrixMarket format together with cell metadata, gene names, and the UMAP embedding from R, and assemble the \texttt{.h5ad} object in Python \seenote{8}.

\item Verify the exported file by reading it back. Confirm the matrix orientation is cells $\times$ genes, that values are raw integer counts, that the UMAP embedding is present, and that per-cell-type counts are unchanged. Here the exported object was $14{,}792 \times 25{,}464$ with a maximum count of 525.
\end{enumerate}

\subsection{Preparing the regulator set}

Load the transcription factor list, strip transcript suffixes, restrict to genes present in the expression matrix, and apply a detection threshold. Of 1,827 transcription factors, 1,370 were present in the RNA assay and 1,022 were detected in at least 50 singlet nuclei, spanning 54 families. This is the regulator set used for inference. Transcription factors absent from the matrix were enriched for families with organ-restricted expression \seenote{19}.

\subsection{Network inference}

\begin{enumerate}[leftmargin=*]
\item For each target cell type, subset the expression matrix to that cell type's nuclei.

\item Configure parallel execution explicitly rather than accepting library defaults. Cap the thread pools used by the numerical libraries before importing them, and construct the dask cluster with a stated number of workers and one thread per worker \seenote{3}:

\begin{lstlisting}[language=Python]
import os
os.environ["OMP_NUM_THREADS"] = "1"
os.environ["MKL_NUM_THREADS"] = "1"
os.environ["OPENBLAS_NUM_THREADS"] = "1"

import dask
dask.config.set({"dataframe.query-planning": False})
\end{lstlisting}

\item Run GRNBoost2 with the regulator list as candidate transcription factors and all genes as candidate targets, with the random seed fixed at 42.

\item Submit each cell type as a separate batch job so that a failure in one does not lose the others. Each job requested one node, 64 cores, 64~GB of memory, and 64 dask workers with one thread each.

\item Write the resulting edge table for each cell type with columns for transcription factor, target, and importance.
\end{enumerate}

Runtimes, memory use, and edge counts are given in Table~\ref{tab:runs}. All three jobs completed in under seven minutes with peak memory below 16~GB. Runtime did not scale with nuclei count at this range \seenote{20}.

\subsection{Selecting transcription factors for visualization}

\begin{enumerate}[leftmargin=*]
\item Identify marker genes for each cell type by Wilcoxon rank-sum differential expression against all other cell types, computed on a normalized and log-transformed copy of the matrix while leaving the stored raw counts unmodified. Retain the top 50 markers per cell type by ascending adjusted \textit{p}-value.

\item For each transcription factor, sum its GRNBoost2 importance across all edges directed at those marker genes.

\item Rank transcription factors by summed importance and retain the top ten per cell type.
\end{enumerate}

This criterion is automatic and requires no manual curation, which suits an agent-directed workflow. The selected transcription factors are listed in Table~\ref{tab:tfs}. Summed importance is a valid ranking statistic within a fixed target set but is not an absolute score \seenote{11}.

\subsection{Figures}

Four figures were generated at 300~dpi in both PNG and PDF format:

\begin{enumerate}[leftmargin=*]
\item A UMAP embedding coloured by cell type, using coordinates from the published object (Fig.~\ref{fig:umap}).
\item Feature plots showing expression of the selected transcription factors on the same embedding (Fig.~\ref{fig:features}).
\item A dot plot of literature-curated root marker genes across all 27 cell types (Fig.~\ref{fig:dotplot}). Of the 50 rows in the source table, 46 unique genes were plotted \seenote{21}.
\item Bipartite network plots for each target cell type, showing the ten selected transcription factors and their five highest-weighted marker targets, with edge width scaled by importance and transcription factor nodes coloured by family (Fig.~\ref{fig:networks}). Edges whose target is itself a selected transcription factor are drawn in a distinct style \seenote{12}.
\end{enumerate}

Figures~\ref{fig:umap} and \ref{fig:dotplot} reproduce features of the published atlas \cite{baumgart2025} and confirm that the exported matrix and labels are faithful to the source. Figures~\ref{fig:features} and \ref{fig:networks} present output not reported in the original publication.

%==============================================================================
\section{Notes}
%==============================================================================

\begin{enumerate}[leftmargin=*]

%--- Environment ---
\item \textbf{pySCENIC 0.12.1 requires setuptools below version 81.} Later setuptools releases remove \texttt{pkg\_resources}, on which \texttt{ctxcore} depends. This surfaces as an import error whose wording does not indicate the actual cause. Pin \texttt{setuptools<81} when creating the environment.

\item \textbf{arboreto 0.1.6 is incompatible with recent dask releases.} Its \texttt{create\_graph()} function calls \texttt{from\_delayed()} on an always-empty list when \texttt{include\_meta=False}, which is the only mode reachable through the public \texttt{grnboost2()} interface. Older dask versions tolerated this; the dask-expr query-planning backend raises \texttt{TypeError: Must supply at least one delayed object}. Disabling query planning before importing arboreto restores the previous behaviour. We confirmed the failure is independent of input data using a synthetic $50 \times 20$ matrix. Testing a library against trivial synthetic input is an efficient way to separate a library defect from a data problem, and is a step an agent performs readily.

\item \textbf{Cap numerical library thread pools before configuring parallel execution.} With default settings on a 12-core workstation, dask created four worker processes and each worker's gradient-boosting fits independently attempted to use the full core count through OpenMP and BLAS thread pools. The resulting contention caused workers to miss scheduler heartbeats, enter restart loops, and fail the entire run after 8~h 16~min with no output. Setting \texttt{OMP\_NUM\_THREADS}, \texttt{MKL\_NUM\_THREADS}, and \texttt{OPENBLAS\_NUM\_THREADS} to 1 and constructing the cluster explicitly resolves this. The symptom --- a long-running job producing no error until it fails completely --- is easily mistaken for a data problem.

\item \textbf{Verify the environment on synthetic data before running on real data.} A GRNBoost2 run on 50 cells, 20 genes, and 5 transcription factors should return exactly 95 edges ($5 \times 19$ non-self pairs). This takes seconds and confirms that all three constraints above are correctly applied. Two of the three failures described in \textbf{Notes~1 and 2} were caught this way rather than after hours of computation.

\item \textbf{Use resumable downloads for large files.} Transfers of the 1.22~GB expression object failed repeatedly with \texttt{curl: (56) schannel: server closed abruptly}. Adding \texttt{-C "-"} and retry options allows an interrupted transfer to resume rather than restart. Verify the byte count after download; a truncated file appears normal until R fails to parse it. Storing the project inside a synchronized cloud folder is an additional source of interruption for files of this size.

%--- Platform ---
\item \textbf{Direct the agent explicitly not to run computation on login nodes.} An agent given a task on a cluster will otherwise execute it wherever it currently is. Stating the constraint in the prompt, and requiring a batch submission script as an output, produces a scheduler script that is itself a better reproducibility artifact than an interactive session.

\item \textbf{Consider the platform before beginning.} We developed this pipeline on a Windows workstation and encountered three platform-specific obstacles before any analysis ran: a malformed certificate in the Windows certificate store prevented import of dask's scheduler through a Windows-only defect in the Python standard library's \texttt{ssl} module; the thread oversubscription in \textbf{Note~3}; and an unattributable system reboot that terminated a multi-hour run. Migrating to a Linux cluster eliminated the first, made the second straightforward to configure, and removed the third. On the cluster the complete inference for all three cell types finished in under twenty minutes. Readers with access to institutional computing should use it from the outset.

\item \textbf{SeuratDisk may be unavailable, and a two-step export is a reliable alternative.} Where \texttt{SaveH5Seurat} cannot be used, writing the counts matrix in MatrixMarket format together with metadata, gene names, and the embedding, then assembling the AnnData object in Python, is transparent and avoids depending on a package with known Seurat~5 compatibility issues. Always verify the result by reading it back and confirming orientation, value type, and per-group counts. A transposed matrix produces no error and fails only much later.

%--- Data handling ---
\item \textbf{Check which assay a Seurat object reports by default.} The default assay of the published object is \texttt{SCT}. Calling \texttt{nrow()} on the object therefore returns 23,974 features rather than the 25,464 in the \texttt{RNA} assay used for inference. The agent initially reported the SCT figure, detected the discrepancy against the value stated in the prompt, and corrected it. Without a stated expectation the wrong feature count would have propagated into the manuscript without producing any error.

\item \textbf{Inspect all cell type annotation columns before choosing one.} This object contains a second annotation column, \texttt{cluster\_label.long}, carrying labels transferred from a leaf reference (including mesophyll and guard cell) together with several thousand missing values. It is inappropriate for a root dataset. Where more than one annotation column exists, confirm which the authors intended for the tissue in question.

%--- Interpreting the output ---
\item \textbf{GRNBoost2 importance is not an absolute or probabilistic score.} Reading the arboreto 0.1.6 source, \texttt{grnboost2()} fits one \texttt{GradientBoostingRegressor} per target gene with \texttt{subsample=0.9}, which enables an out-of-bag early-stopping heuristic with a 25-round trailing window. Each reported importance is scikit-learn's \texttt{feature\_importances\_} --- normalized to sum to 1.0 across all 1,022 predictors, \textit{separately for each target's own model} --- multiplied by the number of boosting rounds actually used. We confirmed this empirically: per-target importance totals are near-exact integers ranging from 25 to 63 (median 27), with the lower bound equal to the early-stopping window length. Consequently, summed importance ranks transcription factors validly within a fixed target set, as used here, but magnitudes are not comparable across different marker sets or cell types, because each target's contribution is rescaled by its own convergence depth. We suggest stating this explicitly wherever importance values are reported.

\item \textbf{Co-expression inference cannot distinguish direction or causality.} In all three cell types, at least one selected transcription factor had among its highest-weighted targets a gene that was itself a selected transcription factor for the same cell type. In mature-endodermis the relationship is reciprocal: \texttt{SbiRTX430.09G017700} $\rightarrow$ \texttt{SbiRTX430.09G017600} at importance 5.41, and the reverse at 4.59, between two MYB genes co-expressed at $r = 0.56$ across 282 nuclei. Their locus identifiers differ by 100 on chromosome 9, consistent with adjacent genes under this annotation's numbering convention, although we did not confirm this against genome coordinates. Two co-expressed paralogs each predicting the other equally well is the expected behaviour of a method operating on expression correlation alone. We retained these edges in the figures and marked them, as they illustrate the limitation more concretely than a general statement.

\item \textbf{Gene identifier namespaces must be validated empirically, not inferred from format.} The RTx430 and BTx623 sorghum identifiers are structurally parallel --- for example \texttt{Sobic.004G266200} and \texttt{SbiRTX430.04G278400} --- which invites the assumption that one can be converted to the other by string manipulation. Testing that assumption against 48 known pairs produced a single match. Chromosome assignment corresponds between the assemblies but gene numbering does not, and the offset is neither constant nor monotonic. Where a resource reports identifiers in two namespaces, use those pairs as ground truth before adopting any conversion.

\item \textbf{Where no cross-reference exists, annotate directly rather than converting.} Submitting the proteome of the assembly actually used by the expression data to the PlantTFDB prediction server returns assignments natively in the required namespace and applies the same family assignment rules as the precomputed lists. We first pursued an alternative route, transferring transcription factor identity from Arabidopsis through orthogroups, which recovered only 939 regulators --- roughly half --- and systematically under-recovered lineage-expanded families such as FAR1 and B3. As a check on the direct annotation, family composition agreed closely with the precomputed BTx623 list (bHLH 168 versus 174; ERF 147 versus 150; MYB 130 versus 130; bZIP 97 versus 97), which is the expected outcome for two independent annotations of two assemblies of the same species. Note also that PlantTFDB returns prediction results to a temporary session directory; save the output immediately and record the job identifier and date.

%--- Agent-specific ---
\item \textbf{Agent-directed pipelines are not deterministic in the way script-based pipelines are.} The same prompt may produce different code on different runs, and GRNBoost2 is itself stochastic and requires an explicit seed. Reproducibility therefore rests on the prompt, the pinned environment, and the fixed seed together, and the prompt must specify outputs precisely enough that different implementations produce equivalent results. We regard this as the principal limitation of the approach.

\item \textbf{The prompt file must be revised as the run proceeds.} Several parameters were absent from the initial prompt because their necessity emerged only during execution --- the dask configuration, the thread caps, the AnnData assembly fallback, and the scheduler resource request. If these are recorded only in the generated scripts, the prompt ceases to be a sufficient artifact. We updated \texttt{prompt.md} at each such point and treated divergence between the prompt and the executed scripts as a defect requiring correction.

\item \textbf{State expected intermediate values in the prompt.} The most useful lines in our prompt specified that singlet filtering should yield 14,792 nuclei and that 1,022 regulators should survive detection filtering. Every checkpoint matched on the cluster run, which gave immediate confidence that the migrated pipeline was faithful. Where a count did not match, the agent stopped and reported rather than proceeding \seenote{9}. Without stated expectations, an agent has no basis for distinguishing a plausible-looking wrong number from a right one.

%--- Biological / analytical ---
\item \textbf{Doublet filtering affects cell types unequally.} Overall retention was 76.6\%, but the three target cell types differed: exodermis 85\% (315 of 371), dividing cells 84\% (801 of 950), and mature-endodermis 65\% (282 of 434). The endodermis is spatially adjacent to the abundant cortex population, which plausibly explains its elevated doublet rate. Where a target cell type loses substantially more nuclei than the dataset average, the resulting network rests on a correspondingly smaller sample.

\item \textbf{Transcription factors absent from the expression matrix are informative.} Of 1,827 annotated transcription factors, 457 were absent from the RNA assay, enriched for M-type MADS, WOX, and MIKC MADS families --- associated with floral organ identity and meristem maintenance, and not expected in three-day-old seedling root. This pattern is a useful check that the annotation behaves sensibly.

\item \textbf{Runtime is dominated by the regulator $\times$ target grid, not by cell count.} At this scale the three inference jobs took 382, 291, and 355 seconds for 282, 801, and 315 nuclei respectively; the largest cell type ran fastest. Cost is governed by the fixed $1{,}022 \times 25{,}464$ regression grid and scheduling overhead. Sizing a scheduler request from cell count alone will over-request substantially.

\item \textbf{Expect some published marker genes to be absent from a processed object.} Of the 50 rows in the marker table, one was an exact duplicate and three genes (\texttt{SbiRTX430.10G175100}, \texttt{SbiRTX430.08G154200}, \texttt{SbiRTX430.06G155500}) were absent from both the RNA and SCT assays of the source object, leaving 46 plotted. We verified their absence against the original object rather than the exported matrix, confirming this was not introduced by the export. Likely explanations are annotation-version differences or undocumented upstream filtering. The source table also reused one gene alias for two distinct identifiers, which must be disambiguated before plotting.

\item \textbf{Exact ties in importance values arise from identical target expression vectors.} Between 5.2\% and 13.2\% of edges in each network share a bit-for-bit identical importance value with two or more other edges from the same transcription factor. Inspecting one 19-way tie block, all 19 target genes were detected in exactly one nucleus --- the same nucleus --- with a count of 1 and zeros elsewhere, making their expression vectors byte-for-byte identical. Identical inputs to independent deterministic fits necessarily produce identical outputs. Below a certain detection rate, sparse expression vectors collapse into a small number of distinct patterns, and any targets sharing a pattern must tie. This is a property of sparse count matrices rather than of the inference method. It did not affect the reported rankings: no top-ranked transcription factor derived any importance from tie-block edges, because the top-50 differentially expressed markers are by construction the most robustly expressed genes and structurally avoid the regime where ties occur.

\item \textbf{Maintain a run log and require the agent to update it.} We maintained \texttt{logs/run\_log.md} recording package versions, parameters, scheduler job identifiers, runtimes, checkpoint dimensions, and each deviation with its justification. Most items in this section derive from that log. An agent will maintain such a log if instructed to; it will not do so unprompted.

\item \PLACEHOLDER{Add more notes here.}

\end{enumerate}

%==============================================================================
\section*{Competing Interests}
%==============================================================================

The authors have no conflicts of interest to declare that are relevant to the content of this chapter.

%==============================================================================
\section*{Acknowledgments}
%==============================================================================

\PLACEHOLDER{Funding sources --- confirm with Dr. Li.} The authors acknowledge Advanced Research Computing at Virginia Tech for providing computational resources and technical support.

%==============================================================================
\section*{References}
%==============================================================================

\PLACEHOLDER{ALL REFERENCES BELOW REQUIRE VERIFICATION --- }

\begin{thebibliography}{99}
\setlength{\itemsep}{2pt}

\bibitem{ryu2019} Ryu KH, Huang L, Kang HM, Schiefelbein J (2019) Single-cell RNA sequencing resolves molecular relationships among individual plant cells. Plant Physiol 179:1444--1456

\bibitem{denyer2019} Denyer T, Ma X, Klesen S et al (2019) Spatiotemporal developmental trajectories in the Arabidopsis root revealed using high-throughput single-cell RNA sequencing. Dev Cell 48:840--852

\bibitem{dorrity2021} Dorrity MW, Alexandre CM, Hamm MO et al (2021) The regulatory landscape of Arabidopsis thaliana roots at single-cell resolution. Nat Commun 12:3334

\bibitem{shahan2022} Shahan R, Hsu C-W, Nolan TM et al (2022) A single-cell Arabidopsis root atlas reveals developmental trajectories in wild-type and cell identity mutants. Dev Cell 57:543--560

\bibitem{marand2021} Marand AP, Chen Z, Gallavotti A, Schmitz RJ (2021) A cis-regulatory atlas in maize at single-cell resolution. Cell 184:3041--3055

\bibitem{kulkarni2018} Kulkarni SR, Vaneechoutte D, Van de Velde J, Vandepoele K (2018) TF2Network: predicting transcription factor regulators and gene regulatory networks in Arabidopsis using publicly available binding site information. Nucleic Acids Res 46:e31

\bibitem{grant2011} Grant CE, Bailey TL, Noble WS (2011) FIMO: scanning for occurrences of a given motif. Bioinformatics 27:1017--1018

\bibitem{song2020} Song Q, Lee J, Akter S, Rogers M, Grene R, Li S (2020) Prediction of condition-specific regulatory genes using machine learning. Nucleic Acids Res 48:e62

\bibitem{moerman2019} Moerman T, Aibar Santos S, Bravo Gonz\'alez-Blas C et al (2019) GRNBoost2 and Arboreto: efficient and scalable inference of gene regulatory networks. Bioinformatics 35:2159--2161

\bibitem{vandesande2020} Van de Sande B, Flerin C, Davie K et al (2020) A scalable SCENIC workflow for single-cell gene regulatory network analysis. Nat Protoc 15:2247--2276

\bibitem{chau2023} Chau T, Timilsena P, Li S (2023) Gene regulatory network modeling using single-cell multi-omics in plants. In: Kaufmann K, Vandepoele K (eds) Plant gene regulatory networks: methods and protocols. Methods in molecular biology, vol 2698. Springer, New York, pp 259--275

\bibitem{baumgart2025} Baumgart LA, Greenblum SI, Morales-Cruz A, Wang P et al (2025) Recruitment, rewiring and deep conservation in flowering plant gene regulation. Nat Plants 11(8):1514--1527

\bibitem{tian2020} Tian F, Yang DC, Meng YQ, Jin JP, Gao G (2020) PlantRegMap: charting functional regulatory maps in plants. Nucleic Acids Res 48(D1):D1104--D1113

\bibitem{jin2017} Jin JP, Tian F, Yang DC et al (2017) PlantTFDB 4.0: toward a central hub for transcription factors and regulatory interactions in plants. Nucleic Acids Res 45(D1):D1040--D1045

\bibitem{hao2024} Hao Y, Stuart T, Kowalski MH et al (2024) Dictionary learning for integrative, multimodal and scalable single-cell analysis. Nat Biotechnol 42:293--304

\bibitem{wolf2018} Wolf FA, Angerer P, Theis FJ (2018) SCANPY: large-scale single-cell gene expression data analysis. Genome Biol 19:15

\bibitem{aibar2017} Aibar S, Gonz\'alez-Blas CB, Moerman T et al (2017) SCENIC: single-cell regulatory network inference and clustering. Nat Methods 14:1083--1086

\bibitem{dask2016} Dask Development Team (2016) Dask: library for dynamic task scheduling. \url{https://dask.org}. Accessed \PLACEHOLDER{date}

\bibitem{agent} \PLACEHOLDER{Reference for the coding agent, or a statement in the Introduction that no peer-reviewed reference is available}

\end{thebibliography}

%==============================================================================
\section*{Tables}
%==============================================================================

\begin{table}[htbp]
\centering
\begin{singlespace}
\caption{Nuclei per cell type after singlet filtering. Cell types used for network inference are shown in bold.}
\label{tab:celltypes}
\small
\begin{tabular}{lr lr lr}
\toprule
Cell type & $n$ & Cell type & $n$ & Cell type & $n$ \\
\midrule
cortex & 1589 & early-epidermis & 599 & \textbf{mature-endodermis} & \textbf{282} \\
pericycle & 1362 & cortical sclerenchyma & 575 & cortical aerenchyma & 281 \\
meristem & 1354 & phloem & 511 & phloem/SE & 264 \\
atrichoblast & 1028 & trichoblast & 504 & elongating-endodermis & 202 \\
lignified-xylem & 818 & LRC & 470 & mature-xylem & 189 \\
\textbf{dividing cells} & \textbf{801} & early-cortex & 438 & sclerenchyma & 151 \\
elongating-xylem & 684 & early-stele & 412 & lignified-endodermis & 133 \\
s-phase & 633 & crown root initials & 346 & senescent cortex & 129 \\
procambium & 605 & \textbf{exodermis} & \textbf{315} & columella & 117 \\
\bottomrule
\end{tabular}
\end{singlespace}
\end{table}

\begin{table}[htbp]
\centering
\begin{singlespace}
\caption{GRNBoost2 batch jobs. All jobs used 1,022 regulators, 25,464 candidate targets, seed 42, and 64 dask workers with one thread each, on one node with 64 cores and 64~GB of memory.}
\label{tab:runs}
\small
\begin{tabular}{lrrrrr}
\toprule
Cell type & Nuclei & Elapsed & Peak memory & Edges & Max importance \\
\midrule
mature-endodermis & 282 & 6\,min 56\,s & 15.5 GB & 1,246,780 & 14.88 \\
dividing cells    & 801 & 5\,min 29\,s & 16.0 GB & 1,563,314 & 15.20 \\
exodermis         & 315 & 6\,min 24\,s & 15.4 GB & 1,380,256 & 14.19 \\
\bottomrule
\end{tabular}
\end{singlespace}
\end{table}

\begin{table}[htbp]
\centering
\begin{singlespace}
\caption{Transcription factors selected for each cell type, ranked by GRNBoost2 importance summed across that cell type's top 50 marker genes. Summed importance is comparable between transcription factors within a cell type but not between cell types \seenote{11}.}
\label{tab:tfs}
\small
\begin{tabular}{clll r}
\toprule
Rank & Cell type & Transcription factor & Family & Summed importance \\
\midrule
1  & mature-endodermis & SbiRTX430.09G017700 & MYB    & 74.81 \\
2  & mature-endodermis & SbiRTX430.09G017600 & MYB    & 25.02 \\
3  & mature-endodermis & SbiRTX430.04G243200 & ERF    & 21.80 \\
4  & mature-endodermis & SbiRTX430.02G238700 & HD-ZIP & 20.28 \\
5  & mature-endodermis & SbiRTX430.01G471500 & HD-ZIP & 19.72 \\
6  & mature-endodermis & SbiRTX430.06G011000 & C2H2   & 18.89 \\
7  & mature-endodermis & SbiRTX430.01G110400 & bHLH   & 17.16 \\
8  & mature-endodermis & SbiRTX430.10G016400 & AP2    & 15.06 \\
9  & mature-endodermis & SbiRTX430.04G299700 & Dof    & 13.26 \\
10 & mature-endodermis & SbiRTX430.09G149400 & NAC    & 12.24 \\
\midrule
1  & dividing cells & SbiRTX430.10G018700 & GRAS        & 73.84 \\
2  & dividing cells & SbiRTX430.09G219200 & WRKY        & 54.56 \\
3  & dividing cells & SbiRTX430.09G224300 & C2H2        & 39.35 \\
4  & dividing cells & SbiRTX430.06G052500 & FAR1        & 35.32 \\
5  & dividing cells & SbiRTX430.03G428400 & C2H2        & 20.26 \\
6  & dividing cells & SbiRTX430.09G130300 & AP2         & 19.06 \\
7  & dividing cells & SbiRTX430.07G054700 & MYB\_related & 17.96 \\
8  & dividing cells & SbiRTX430.09G085600 & MYB         & 17.63 \\
9  & dividing cells & SbiRTX430.01G110400 & bHLH        & 15.15 \\
10 & dividing cells & SbiRTX430.10G044200 & DBB         & 11.42 \\
\midrule
1  & exodermis & SbiRTX430.03G038000 & NAC         & 74.51 \\
2  & exodermis & SbiRTX430.02G190100 & ERF         & 27.78 \\
3  & exodermis & SbiRTX430.06G011000 & C2H2        & 26.64 \\
4  & exodermis & SbiRTX430.04G294200 & MYB\_related & 25.09 \\
5  & exodermis & SbiRTX430.09G017700 & MYB         & 24.56 \\
6  & exodermis & SbiRTX430.02G211900 & AP2         & 23.62 \\
7  & exodermis & SbiRTX430.04G243200 & ERF         & 19.41 \\
8  & exodermis & SbiRTX430.08G120400 & WRKY        & 17.26 \\
9  & exodermis & SbiRTX430.03G310600 & WRKY        & 14.40 \\
10 & exodermis & SbiRTX430.09G219200 & WRKY        & 14.30 \\
\bottomrule
\end{tabular}
\end{singlespace}
\end{table}

\clearpage

%==============================================================================
\section*{Figures}
%==============================================================================

% NOTE ON SUBMISSION FORMAT
% Springer requires figures as SEPARATE files, not embedded in the manuscript,
% with legends listed after the References. The figures below are embedded for
% drafting convenience only.
%
% To switch to submission format, change the toggle below to \draftfiguresfalse.
% This hides the images and prints legend text only.
%
% Upload your PNG files to the Overleaf project root and they will appear
% automatically. Until then, a grey placeholder box is shown instead.

\begin{figure}[htbp]
\centering
\FIGBOX{figures/fig1_workflow}{0.85}
\begin{singlespace}
\caption{Workflow of the agent-directed pipeline. Boxes indicate the stages of
the protocol with the corresponding subheading below each. Stages above the
dashed line are performed by the analyst before the agent is invoked; stages
below are executed by the agent from the prompt file.
\PLACEHOLDER{This figure does not yet exist --- create a schematic modelled on
Fig.~1 of ref.~\cite{chau2023}. Save as fig1\_workflow.png.}}
\label{fig:workflow}
\end{singlespace}
\end{figure}

\begin{figure}[htbp]
\centering
\FIGBOX{figures/fig1_umap_celltypes}{0.85}
\begin{singlespace}
\caption{UMAP embedding of 14,792 singlet nuclei from the sorghum root atlas,
coloured by cell type. Coordinates and cell type labels are those supplied with
the published object \cite{baumgart2025}; this panel reproduces the published
atlas and confirms that the exported expression matrix and metadata are
faithful to the source.}
\label{fig:umap}
\end{singlespace}
\end{figure}

\begin{figure}[htbp]
\centering
\FIGBOX{figures/fig2_tf_featureplots}{1.0}
\begin{singlespace}
\caption{Expression of selected transcription factors projected onto the UMAP
embedding of Fig.~\ref{fig:umap}. Rows correspond to the three cell types
analysed; columns to the ten transcription factors selected for that cell type,
ordered by rank. Panel titles give rank, gene identifier, and transcription
factor family. Colour indicates log-normalized expression; scales are
independent per panel.}
\label{fig:features}
\end{singlespace}
\end{figure}

\begin{figure}[htbp]
\centering
\FIGBOX{figures/fig3_marker_dotplot}{0.8}
\begin{singlespace}
\caption{Dot plot of literature-curated sorghum root marker genes across all 27
cell types. Dot size indicates the proportion of nuclei in which the gene is
detected; colour indicates mean expression scaled per gene. Of the 50 rows in
the source table \cite{baumgart2025}, one was a duplicate and three genes were
absent from the expression object, leaving 46 genes plotted \seenote{21}. Gene
labels use the aliases given in the source table; one alias reused for two
identifiers is disambiguated with a gene identifier suffix.}
\label{fig:dotplot}
\end{singlespace}
\end{figure}

\begin{figure}[htbp]
\centering
\FIGBOX{figures/fig4_networks_mature-endodermis}{0.95}\\[4pt]
{\small (\textbf{a}) mature-endodermis}\\[10pt]
\FIGBOX{figures/fig4_networks_dividing_cells}{0.95}\\[4pt]
{\small (\textbf{b}) dividing cells}\\[10pt]
\FIGBOX{figures/fig4_networks_exodermis}{0.95}\\[4pt]
{\small (\textbf{c}) exodermis}
\begin{singlespace}
\caption{Inferred transcription factor--target associations for
(\textbf{a}) mature-endodermis, (\textbf{b}) dividing cells, and
(\textbf{c}) exodermis. Transcription factors (left) are the ten with the
highest summed GRNBoost2 importance across that cell type's top 50 marker
genes; targets (right) are their five highest-weighted marker targets each.
Edge width is proportional to GRNBoost2 importance. Transcription factor node
colour indicates family. Crimson dashed edges mark targets that are themselves
selected transcription factors for the same cell type; in mature-endodermis
this relationship is reciprocal between two co-expressed MYB genes. Edges
represent co-expression associations inferred from expression data alone and
are not validated regulatory relationships \seenotes{11 and 12}.}
\label{fig:networks}
\end{singlespace}
\end{figure}

\clearpage

\clearpage

%==============================================================================
\section*{Submission checklist}
%==============================================================================

\PLACEHOLDER{Delete this section before submission.}

\begin{itemize}[leftmargin=*]
\item[$\square$] Confirm final author list --- authors cannot be added after production begins
\item[$\square$] Cover page with names, affiliations, and corresponding author email
\item[$\square$] Verify every reference
\item[$\square$] Create Fig.~1 workflow schematic
\item[$\blacksquare$] Create GitHub repository and insert URL in \textbf{Subheading~2}
\item[$\square$] Figure files as separate labelled files (Fig 1, Fig 2, \ldots), TIFF, 300~dpi minimum, 600~dpi for combined line art and halftone
\item[$\square$] Alt text for every figure as a separate Excel file (required under the EU Accessibility Act)
\item[$\square$] Signed Author Agreement from the corresponding author
\item[$\square$] Permissions --- confirm with the volume editor whether Figs.~2 and 4 require permission from \textit{Nature Plants}, as they reproduce features of the published atlas. Permissions take up to three months; raise this immediately.
\item[$\square$] Record the Claude Code version used
\item[$\square$] Confirm with the volume editor that LaTeX submission is acceptable for this volume
\end{itemize}

\end{document}
